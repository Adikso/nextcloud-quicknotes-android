<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:id="@+id/categories" android:checkableBehavior="single">
        <item
            android:id="@+id/all"
            android:icon="@drawable/ic_home"
            android:title="@string/all_notes"
            android:checked="true" />
        <item
            android:id="@+id/pinned"
            android:icon="@drawable/ic_pinned"
            android:visible="false"
            android:title="@string/pinned" />
        <item
            android:id="@+id/shared_with_you"
            android:icon="@drawable/ic_shared_grey"
            android:visible="false"
            android:title="@string/shared_with_you" />
        <item
            android:id="@+id/shared_with_others"
            android:icon="@drawable/ic_shared_grey"
            android:visible="false"
            android:title="@string/shared_with_others" />
    </group>
    <group android:visible="false">
        <item android:id="@+id/tags_outer" android:title="@string/tags">
            <menu>
                <group android:id="@+id/tags" android:checkableBehavior="single">
                </group>
            </menu>
        </item>
    </group>
    <group android:id="@+id/other" android:checkableBehavior="none">
        <item
            android:id="@+id/about"
            android:icon="@drawable/ic_info_grey"
            android:title="@string/about" />
        <item
            android:id="@+id/switch_account"
            android:icon="@drawable/ic_logout_grey"
            android:title="@string/switch_account" />
    </group>
</menu>